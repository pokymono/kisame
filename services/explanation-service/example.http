### Health
GET http://localhost:8787/health

### tshark version (ensure tshark is installed / TSHARK_PATH set)
GET http://localhost:8787/tshark/version

### Upload a PCAP (replace with a real file path in your HTTP client)
# Many REST clients support binary file bodies; this is a placeholder example.
POST http://localhost:8787/pcap
Content-Type: application/octet-stream
x-filename: capture.pcapng

< ./path/to/capture.pcapng

### AnalyzePCAP tool (use the session_id from upload)
POST http://localhost:8787/tools/analyzePcap
Content-Type: application/json

{
  "session_id": "REPLACE_ME",
  "max_packets": 5000,
  "sample_frames_per_session": 8
}
